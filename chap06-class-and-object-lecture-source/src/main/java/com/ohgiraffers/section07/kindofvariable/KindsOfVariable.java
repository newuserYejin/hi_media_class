package com.ohgiraffers.section07.kindofvariable;

public class KindsOfVariable {

    // 필드 == 멤버변수(틀래스가 가진 변수) == 전역변수

    // non-static 필드 -> 인스턴스 변수

    // static 필드 = 정적 필드(클래스 변수)

    // 매개변수도 일종의 지역변수 -> stack 영역에 생성
}
